# Phase 1B Registry Fixtures

`valid-parameterized.json` is the schema-valid baseline for Task 4 semantic tests. Tests mutate an in-memory copy to isolate each semantic invariant and use temporary repository roots for filesystem containment cases.
